using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugingAdd : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("±Ë¿Á»Ò µ⁄¡Æ∂Û");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
